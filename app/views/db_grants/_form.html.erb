<%= form_for(@db_grant) do |f| %>
  <% if @db_grant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@db_grant.errors.count, "error") %> prohibited this db_grant from being saved:</h2>

      <ul>
      <% @db_grant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :db %><br>
    <%= f.text_field :db %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
